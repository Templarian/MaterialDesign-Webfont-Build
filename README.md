# Webfont Custom Build - Material Design Icons

Custom Builds of the Webfont.

## Usage

Clone repo, run `npm install`, and create (empty) `icons` folder.

## Commands

- `npm run buildIcons` - Build /icons folder
